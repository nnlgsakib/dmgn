---
phase: 6
plan: 2
title: "OpenTelemetry + Structured Logging"
wave: 2
depends_on: [06-01]
files_modified:
  - pkg/observability/otel.go
  - pkg/observability/metrics.go
  - pkg/observability/logging.go
  - pkg/observability/otel_test.go
  - internal/config/config.go
  - internal/cli/start.go
  - internal/cli/mcp.go
  - go.mod
  - go.sum
requirements_addressed: [INTG-01, INTG-02]
autonomous: true
estimated_tasks: 5
---

# Plan 06-02: OpenTelemetry + Structured Logging

<objective>
Add full OpenTelemetry observability (traces, metrics, logs) with slog bridge, dual log output (stderr + rotating file), and instrument key operations across the codebase.
</objective>

## Tasks

<task id="06-02-01" title="Add OTel and logging dependencies">
<read_first>
- go.mod
</read_first>
<action>
Add dependencies:
```
go get go.opentelemetry.io/otel
go get go.opentelemetry.io/otel/sdk/trace
go get go.opentelemetry.io/otel/sdk/metric
go get go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc
go get go.opentelemetry.io/otel/exporters/otlp/otlpmetric/otlpmetricgrpc
go get go.opentelemetry.io/otel/exporters/stdout/stdouttrace
go get go.opentelemetry.io/contrib/bridges/otelslog
go get gopkg.in/natefinished/lumberjack.v2
```
</action>
<acceptance_criteria>
- go.mod contains `go.opentelemetry.io/otel`
- go.mod contains `gopkg.in/natefinished/lumberjack.v2`
- `go mod tidy` exits 0
</acceptance_criteria>
</task>

<task id="06-02-02" title="Create observability package — OTel setup">
<read_first>
- pkg/observability/otel.go (new file)
- internal/config/config.go (config pattern)
</read_first>
<action>
Create `pkg/observability/otel.go` with:

1. `OTelConfig` struct: `ServiceName string`, `OTLPEndpoint string`, `EnableTraces bool`, `EnableMetrics bool`, `LogLevel string`
2. `InitOTel(ctx context.Context, cfg OTelConfig) (*OTelProviders, error)`:
   - Create resource with service name, version
   - If `OTLPEndpoint != ""`: create OTLP gRPC exporters for traces and metrics
   - If `OTLPEndpoint == ""`: create noop exporters (no external dependency required)
   - Create and register TracerProvider
   - Create and register MeterProvider
   - Return `*OTelProviders` for shutdown
3. `OTelProviders` struct with `Shutdown(ctx) error` that stops both providers
4. `Tracer(name string)` — convenience wrapper for `otel.Tracer(name)`
5. `Meter(name string)` — convenience wrapper for `otel.Meter(name)`
</action>
<acceptance_criteria>
- `pkg/observability/otel.go` exists with `InitOTel` function
- `OTelProviders` has `Shutdown` method
- `go build ./pkg/observability/...` exits 0
</acceptance_criteria>
</task>

<task id="06-02-03" title="Create metrics and logging modules">
<read_first>
- pkg/observability/metrics.go (new file)
- pkg/observability/logging.go (new file)
</read_first>
<action>
Create `pkg/observability/metrics.go`:
1. `DMGNMetrics` struct holding all metric instruments:
   - `MemoryCount` (Int64Counter) — `dmgn.memory.count`
   - `QueryLatency` (Float64Histogram) — `dmgn.query.latency_ms`
   - `PeerCount` (Int64UpDownCounter) — `dmgn.peer.count`
   - `SyncEvents` (Int64Counter) — `dmgn.sync.events`
   - `GossipMessages` (Int64Counter) — `dmgn.gossip.messages`
   - `VectorIndexSize` (Int64UpDownCounter) — `dmgn.vectorindex.size`
2. `NewDMGNMetrics(meter) (*DMGNMetrics, error)` — creates all instruments

Create `pkg/observability/logging.go`:
1. `InitLogging(cfg LogConfig) *slog.Logger`:
   - `LogConfig{Level string, LogDir string, MaxSizeMB int, MaxBackups int, Stderr bool}`
   - Create lumberjack writer for file rotation: `{LogDir}/dmgn.log`
   - Create multi-writer: stderr + lumberjack file (if both enabled)
   - Create slog.JSONHandler with the multi-writer
   - If OTel bridge available, wrap with otelslog handler
   - Return configured `*slog.Logger`
2. `LogLevelFromString(s string) slog.Level` — parse "debug"/"info"/"warn"/"error"
</action>
<acceptance_criteria>
- `pkg/observability/metrics.go` contains `DMGNMetrics` struct with 6 metric instruments
- `pkg/observability/logging.go` contains `InitLogging` returning `*slog.Logger`
- `go build ./pkg/observability/...` exits 0
</acceptance_criteria>
</task>

<task id="06-02-04" title="Wire OTel into startup and MCP serve">
<read_first>
- internal/cli/start.go (daemon startup)
- internal/cli/mcp.go (MCP serve command)
- internal/config/config.go (config struct)
</read_first>
<action>
1. Add config fields to `config.Config`:
   - `OTLPEndpoint string` (json:"otlp_endpoint", default "")
   - `LogLevel string` (json:"log_level", default "info")
   - `LogDir string` — helper method returns `{DataDir}/logs`

2. In `start.go` RunE, after crypto setup:
   - Call `observability.InitOTel(ctx, otelCfg)` 
   - Call `observability.InitLogging(logCfg)`
   - `defer providers.Shutdown(ctx)`
   - Create `DMGNMetrics` and pass to components that need instrumentation

3. In `mcp.go` RunE:
   - Same OTel and logging init
   - Logging to stderr + file (stdout reserved for MCP protocol)
   - Pass logger to MCPServer
</action>
<acceptance_criteria>
- `internal/config/config.go` contains `OTLPEndpoint` and `LogLevel` fields
- `internal/cli/start.go` calls `observability.InitOTel`
- `internal/cli/mcp.go` calls `observability.InitLogging`
- `go build ./...` exits 0
</acceptance_criteria>
</task>

<task id="06-02-05" title="Write observability tests">
<read_first>
- pkg/observability/otel.go
- pkg/observability/metrics.go
- pkg/observability/logging.go
</read_first>
<action>
Create `pkg/observability/otel_test.go`:
1. `TestInitOTelNoEndpoint` — init with empty endpoint, verify noop providers, shutdown clean
2. `TestDMGNMetrics` — create metrics, record values, verify no panics
3. `TestInitLogging` — init logging to temp dir, write log entries, verify file created
4. `TestLogLevelParsing` — verify debug/info/warn/error parsing
5. `TestShutdown` — init providers, shutdown, verify no error
</action>
<acceptance_criteria>
- `pkg/observability/otel_test.go` exists with 5+ test functions
- `go test ./pkg/observability/... -count=1` exits 0
</acceptance_criteria>
</task>

## Verification

```bash
go build ./...
go test ./pkg/observability/... -count=1 -v
```

## must_haves
- OTel traces and metrics exportable to OTLP endpoint
- slog structured logging with JSON output
- Dual log output: stderr + rotating file
- No stdout pollution in MCP mode
