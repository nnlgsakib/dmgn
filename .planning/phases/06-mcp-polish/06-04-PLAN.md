---
phase: 6
plan: 4
title: "Documentation Suite"
wave: 4
depends_on: [06-01, 06-02, 06-03]
files_modified:
  - docs/architecture.md
  - docs/mcp-integration.md
  - docs/api-reference.md
  - docs/cli-reference.md
  - docs/config-reference.md
  - docs/troubleshooting.md
  - examples/claude_desktop_config.json
  - examples/cline_config.json
  - README.md
requirements_addressed: [MCP-05]
autonomous: true
estimated_tasks: 5
---

# Plan 06-04: Documentation Suite

<objective>
Create comprehensive documentation covering architecture, MCP integration, API reference, CLI reference, config reference, and troubleshooting. Update README with MCP quick start. Provide copy-paste config examples for Claude Desktop and Cline.
</objective>

## Tasks

<task id="06-04-01" title="Create architecture and MCP integration docs">
<read_first>
- README.md (current state)
- .planning/PROJECT.md (project vision)
- pkg/mcp/server.go (MCP tool definitions)
- internal/cli/mcp.go (mcp-serve command flags)
</read_first>
<action>
Create `docs/architecture.md`:
- System overview: identity → storage → crypto → network → query → MCP
- Component diagram (text-based): show data flow from AI agent → MCP → storage → network
- Key design decisions summary (from STATE.md decisions list)
- Package map: what each pkg/ and internal/ package does

Create `docs/mcp-integration.md`:
- Prerequisites: `dmgn init` to create identity
- Claude Desktop setup:
  ```json
  {
    "mcpServers": {
      "dmgn": {
        "command": "dmgn",
        "args": ["mcp-serve", "--data-dir", "~/.dmgn"]
      }
    }
  }
  ```
- Cline setup (VS Code extension config)
- Tool reference: all 7 tools with input schemas and example request/response
- Networking mode: when and how to use `--network`
- Troubleshooting MCP-specific issues
</action>
<acceptance_criteria>
- `docs/architecture.md` exists with system overview and component diagram
- `docs/mcp-integration.md` exists with Claude Desktop and Cline setup guides
- Both files contain valid markdown
</acceptance_criteria>
</task>

<task id="06-04-02" title="Create API and CLI reference docs">
<read_first>
- internal/api/handlers.go (REST API endpoints)
- internal/api/server.go (route setup)
- internal/cli/add.go, internal/cli/query.go, internal/cli/start.go, internal/cli/mcp.go (CLI commands)
- cmd/dmgn/main.go (registered commands)
</read_first>
<action>
Create `docs/api-reference.md`:
- Authentication: Bearer token from `dmgn init`
- Endpoints: POST /memory, GET /query, GET /status, GET /peers
- Each endpoint: method, URL, auth, request body/params, response body, curl example
- Error responses

Create `docs/cli-reference.md`:
- All commands: init, start, add, query, status, peers, mcp-serve, backup, restore, export-key, import-key
- Each command: usage, flags, description, examples
- Global flags: --data-dir
</action>
<acceptance_criteria>
- `docs/api-reference.md` exists with all 4 REST endpoints documented
- `docs/cli-reference.md` exists with all CLI commands documented
- Each endpoint/command has at least one example
</acceptance_criteria>
</task>

<task id="06-04-03" title="Create config reference and troubleshooting docs">
<read_first>
- internal/config/config.go (all config fields with defaults)
</read_first>
<action>
Create `docs/config-reference.md`:
- Config file location: `{data-dir}/config.json`
- Every field from `config.Config` struct:
  - Field name (JSON key)
  - Type
  - Default value
  - Description
  - Example
- Organized by section: General, Network, Storage, Query, Sync, MCP, Observability

Create `docs/troubleshooting.md`:
- "No identity found" — run `dmgn init`
- "Failed to connect to peers" — check firewall, bootstrap peers
- "MCP server not responding" — check stdout/stderr separation
- "Query returns no results" — check embeddings, vector index
- "Backup restore failed" — check file integrity, version compatibility
- "High memory usage" — tune vector index, BadgerDB GC
- Log file locations and how to change log level
</action>
<acceptance_criteria>
- `docs/config-reference.md` lists all config fields with defaults
- `docs/troubleshooting.md` has at least 6 common issues
</acceptance_criteria>
</task>

<task id="06-04-04" title="Create example config files">
<read_first>
- pkg/mcp/server.go (MCP server name for config)
</read_first>
<action>
Create `examples/claude_desktop_config.json`:
```json
{
  "mcpServers": {
    "dmgn": {
      "command": "dmgn",
      "args": ["mcp-serve"],
      "env": {}
    }
  }
}
```

Create `examples/cline_config.json`:
```json
{
  "mcpServers": {
    "dmgn": {
      "command": "dmgn",
      "args": ["mcp-serve", "--data-dir", "~/.dmgn"],
      "disabled": false
    }
  }
}
```
</action>
<acceptance_criteria>
- `examples/claude_desktop_config.json` exists with valid JSON
- `examples/cline_config.json` exists with valid JSON
- Both reference `dmgn mcp-serve` command
</acceptance_criteria>
</task>

<task id="06-04-05" title="Update README with MCP section">
<read_first>
- README.md (current content)
- docs/mcp-integration.md (link target)
</read_first>
<action>
Add to README.md after the existing content:

1. **MCP Integration** section:
   - Brief description: "DMGN works as an MCP server for AI agents"
   - Quick start: `dmgn init` → configure Claude Desktop → use tools
   - Link to `docs/mcp-integration.md` for detailed setup
   - List of available MCP tools (one-liner each)

2. **Documentation** section:
   - Links to all docs/ files
   - Brief description of each

3. **Backup & Restore** section:
   - `dmgn backup` / `dmgn restore` usage

Update any outdated sections to reflect Phase 6 completion.
</action>
<acceptance_criteria>
- README.md contains "MCP Integration" section
- README.md links to docs/mcp-integration.md
- README.md lists all 7 MCP tools
- README.md contains "Documentation" section linking to docs/
</acceptance_criteria>
</task>

## Verification

```bash
# Verify all doc files exist
ls docs/architecture.md docs/mcp-integration.md docs/api-reference.md docs/cli-reference.md docs/config-reference.md docs/troubleshooting.md
ls examples/claude_desktop_config.json examples/cline_config.json
# Verify README updated
grep -c "MCP Integration" README.md
```

## must_haves
- Full docs suite in docs/ folder
- MCP integration guide with copy-paste configs
- API and CLI references with examples
- Config reference with all fields
- README updated with MCP section
