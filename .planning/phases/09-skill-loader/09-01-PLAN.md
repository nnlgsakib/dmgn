---
phase: 09-skill-loader
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - pkg/skill/trigger.go
  - pkg/skill/loader.go
  - pkg/skill/SKILL.md
  - pkg/mcp/server.go
  - skill/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - AI agents can detect trigger phrases like "init dmgn", "dmgn context" in conversation
    - When triggered, AI agent receives full skill content (7 tools reference, behavioral protocol)
    - Skill content loads from file at runtime if available
    - If file missing, embedded fallback provides skill content
    - MCP server includes load_skill tool for explicit skill loading
  artifacts:
    - path: pkg/skill/trigger.go
      provides: Trigger detection logic (direct + fuzzy match)
      min_lines: 30
    - path: pkg/skill/loader.go
      provides: Skill content loading with embed fallback
      min_lines: 25
    - path: pkg/mcp/server.go
      provides: load_skill MCP tool
      exports: load_skill tool
    - path: skill/SKILL.md
      provides: Embedded skill content via go:embed
      contains: all 7 tools reference
  key_links:
    - from: pkg/mcp/server.go
      to: pkg/skill/loader.go
      via: skill.Load() function call
      pattern: skill\\.Load
    - from: pkg/skill/loader.go
      to: skill/SKILL.md
      via: go:embed directive
      pattern: go:embed.*SKILL\\.md
---

<objective>
Create a conversational skill-trigger system for DMGN. When users mention "dmgn" in conversation, AI agents can trigger skill loading and receive the full DMGN skill content (all 7 tools reference, behavioral protocol) for automatic context injection.
</objective>

<context>
@.planning/phases/09-skill-loader/09-CONTEXT.md
@.planning/phases/09-skill-loader/09-RESEARCH.md
@skill/SKILL.md
@pkg/mcp/server.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill package with trigger detection and loader</name>
  <files>pkg/skill/trigger.go, pkg/skill/loader.go</files>
  <read_first>
    - skill/SKILL.md (full skill content to embed)
    - pkg/mcp/server.go (tool registration patterns to follow)
  </read_first>
  <action>
Create the pkg/skill directory with two Go files:

1. **pkg/skill/trigger.go** - Trigger detection logic:
   - Define `DirectPatterns` slice with: "init dmgn", "load dmgn", "dmgn context", "init dmgn context"
   - Define `FuzzyKeywords` slice with: "context", "memory", "remember", "skill", "load", "init", "setup", "enable"
   - Implement `Detect(input string) bool` that:
     - Converts input to lowercase for case-insensitive matching
     - Checks if any direct pattern is contained in input
     - If input contains "dmgn" AND any fuzzy keyword, returns true
   - Add comprehensive test cases in trigger_test.go

2. **pkg/skill/loader.go** - Skill loading with embed fallback:
   - Add `//go:embed SKILL.md` directive to embed skill/SKILL.md content
   - Define `embeddedSkill` variable of type `[]byte`
   - Implement `Load() ([]byte, error)` function that:
     - First tries to read `./skill/SKILL.md` using os.ReadFile
     - If file read succeeds, returns file contents
     - If file read fails (any error), returns embeddedSkill as fallback
     - Returns nil error for embedded fallback (never fails)
   - Add test for file-not-found fallback behavior
  </action>
  <acceptance_criteria>
- [ ] pkg/skill/trigger.go exists with Detect() function
- [ ] Direct patterns match: "init dmgn", "load dmgn", "dmgn context", "init dmgn context"
- [ ] Fuzzy match works: "dmgn" + any keyword from FuzzyKeywords
- [ ] pkg/skill/loader.go exists with Load() function
- [ ] go:embed directive embeds SKILL.md content
- [ ] Load() returns embedded content when file missing
- [ ] Tests pass: go test ./pkg/skill/...
  </acceptance_criteria>
  <verify>
    <automated>go test ./pkg/skill/... -v</automated>
  </verify>
  <done>
pkg/skill package created with trigger detection and embedded skill loading. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add load_skill tool to MCP server</name>
  <files>pkg/mcp/server.go</files>
  <read_first>
    - pkg/mcp/server.go (existing tool registration patterns at lines 74-109)
  </read_first>
  <action>
Extend the MCP server in pkg/mcp/server.go to add the load_skill tool:

1. Import the skill package:
   ```go
   "github.com/nnlgsakib/dmgn/pkg/skill"
   ```

2. In the `newServer()` function, add after line 107 (before return server):
   ```go
   mcp.AddTool(server, &mcp.Tool{
       Name:        "load_skill",
       Description: "Load DMGN skill content for agent context. Call this when user mentions DMGN or wants to initialize DMGN capabilities.",
   }, s.handleLoadSkill)
   ```

3. Add handler function after the existing handlers (after line 477):
   ```go
   type LoadSkillInput struct{}

   type LoadSkillOutput struct {
       Content string `json:"content"`
   }

   func (s *MCPServer) handleLoadSkill(ctx context.Context, req *mcp.CallToolRequest, input LoadSkillInput) (*mcp.CallToolResult, LoadSkillOutput, error) {
       content, err := skill.Load()
       if err != nil {
           return nil, LoadSkillOutput{}, fmt.Errorf("failed to load skill: %w", err)
       }
       return nil, LoadSkillOutput{
           Content: string(content),
       }, nil
   }
   ```

4. Ensure the import is added if not present
  </action>
  <acceptance_criteria>
- [ ] MCP server registers load_skill tool
- [ ] handleLoadSkill function exists and calls skill.Load()
- [ ] Returns skill content as JSON
- [ ] Build succeeds: go build ./pkg/mcp/...
- [ ] Existing tests still pass: go test ./pkg/mcp/...
  </acceptance_criteria>
  <verify>
    <automated>go build ./pkg/mcp/... && go test ./pkg/mcp/... -v</automated>
  </verify>
  <done>
MCP server includes load_skill tool. Build succeeds, existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update skill/SKILL.md with trigger detection instructions</name>
  <files>skill/SKILL.md</files>
  <read_first>
    - skill/SKILL.md (existing content to preserve)
  </read_first>
  <action>
Update skill/SKILL.md to include instructions for AI agents to detect triggers and load the skill:

Add a new section at the TOP of the file (after the main title and intro), or as a prominent note:

```
## Trigger Detection

When the user mentions DMGN in conversation, you should load the full skill to provide better assistance.

### When to Load
Call the `load_skill` tool (MCP) when the user says things like:
- "init dmgn", "load dmgn", "dmgn context", "init dmgn context"
- Any message containing "dmgn" + context/memory/remember/skill/setup/enable

### How to Load
1. Call the `load_skill` MCP tool
2. The tool returns the full skill content
3. Use that content to inform your responses about DMGN capabilities

### Why This Matters
Calling load_skill gives you:
- Complete reference to all 7 DMGN tools
- Behavioral protocol for session start/during/end
- Metadata conventions to follow
- Architecture understanding for trust
```
  </action>
  <acceptance_criteria>
- [ ] skill/SKILL.md updated with trigger detection section
- [ ] Section explains when to call load_skill tool
- [ ] Section explains what load_skill provides
- [ ] Original skill content preserved
  </acceptance_criteria>
  <verify>
    <automated>grep -i "load_skill" skill/SKILL.md</automated>
  </verify>
  <done>
SKILL.md updated with trigger detection instructions for AI agents.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → trigger detection | Untrusted user messages processed by Detect() function |
| skill/SKILL.md → embedded content | Skill file loaded at build time via go:embed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-01 | Tampering | pkg/skill/loader.go | mitigate | Validate file path stays within ./skill/ directory (filepath.Clean) |
| T-09-02 | Information Disclosure | skill/SKILL.md | accept | Skill content is intentionally public (AI agent reference) |
| T-09-03 | Denial of Service | pkg/skill/loader.go | mitigate | Add max file size check (50KB limit) before loading |
| T-09-04 | Repudiation | pkg/skill/trigger.go | accept | No audit needed for trigger detection (no security impact) |
</threat_model>

<verification>
- Run go test ./pkg/skill/... to verify trigger detection and loading
- Run go build ./... to verify MCP integration compiles
- Verify load_skill tool is registered (grep for "load_skill" in server.go)
</verification>

<success_criteria>
- pkg/skill package created with trigger detection and embed fallback
- MCP server includes load_skill tool callable by AI agents
- SKILL.md updated with trigger detection instructions
- All code compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-skill-loader/09-01-SUMMARY.md`
</output>
